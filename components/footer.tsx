export default function Footer() {
  return (
    <footer style={{padding: '1rem', textAlign: 'center', opacity: 0.7}}>
      <small> {new Date().getFullYear()} My Library</small>
    </footer>
  );
}